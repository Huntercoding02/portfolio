"use client";
import Link from "next/link";
import { endtext } from "../Utils/endtext";
import type { Project } from "../data/Portfolio";
import { useLanguage } from "@/app/components/LanguageProvider";
type ProjectsProps = { projects: Project[] };
export default function Projects({ projects }: ProjectsProps) { const { language, t } = useLanguage(); return <section className="mt-14"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-[0.2em] text-[#635bff]">{t("selectedWork")}</p><h2 className="mt-2 text-3xl font-black tracking-[-0.04em]">{t("built")}</h2></div><Link href="/projects" className="text-sm font-bold text-[#635bff] hover:underline">{t("allProjects")}</Link></div><div className="mt-6 grid gap-5 md:grid-cols-2">{projects.map((project, index) => <article key={project.id} className="group overflow-hidden rounded-3xl border border-[#e7eaf1] bg-white shadow-sm transition duration-300 hover:-translate-y-1 hover:shadow-xl hover:shadow-[#172033]/10"><div className={`h-40 ${project.image ? "bg-[#b9eee7]" : "bg-[#dcd9ff] p-6"}`}>{project.image ? <img src={project.image} alt={`${language === "th" ? project.nameTh : project.name} preview`} className="h-full w-full object-cover object-top" /> : <span className="text-5xl">{index % 2 === 0 ? "✦" : "↗"}</span>}</div><div className="p-6"><p className="text-sm font-bold text-[#635bff]">{endtext(project.technologies)}</p><h3 className="mt-2 text-2xl font-bold">{language === "th" ? project.nameTh : project.name}</h3><p className="mt-3 leading-7 text-[#5f6b85]">{language === "th" ? project.descriptionTh : project.description}</p><div className="mt-5 flex flex-wrap gap-3"><Link href={`/projects/${project.id}`} className="font-bold text-[#172033] transition group-hover:text-[#635bff]">{t("viewProject")}</Link><a href={project.githubUrl} target="_blank" rel="noreferrer" className="font-bold text-[#635bff] hover:underline">{t("github")}</a></div></div></article>)}</div></section>; }
